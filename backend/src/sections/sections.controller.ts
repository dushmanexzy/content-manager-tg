import { Controller } from '@nestjs/common';

/**
 * SectionsController — устаревший контроллер
 * API для разделов теперь обрабатывается через ApiController
 * Этот файл оставлен для совместимости с модулем
 */
@Controller('sections')
export class SectionsController {}
