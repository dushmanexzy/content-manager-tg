# План редизайна TG-App: Clean White + Dark Mode с Teal акцентами

## 1. Текущая система цветов

### CSS-переменные (App.css)

| Переменная | Текущее значение | Назначение |
|------------|------------------|------------|
| `--tg-theme-bg-color` | `#ffffff` | Основной фон приложения |
| `--tg-theme-text-color` | `#000000` | Основной текст (заголовки, названия) |
| `--tg-theme-hint-color` | `#999999` | Вторичный текст (мета-информация, подсказки) |
| `--tg-theme-link-color` | `#3390ec` | Ссылки, активные элементы навигации |
| `--tg-theme-button-color` | `#3390ec` | Кнопки, акцентные элементы |
| `--tg-theme-button-text-color` | `#ffffff` | Текст на кнопках |
| `--tg-theme-secondary-bg-color` | `#f5f5f5` | Фон карточек, полей ввода |

### Где используются (частота)

| Переменная | Использований | Компоненты |
|------------|---------------|------------|
| `--tg-theme-secondary-bg-color` | ~25 | Карточки, модалки, инпуты, хедер |
| `--tg-theme-hint-color` | ~22 | Мета-текст везде, бордеры, стрелки |
| `--tg-theme-text-color` | ~12 | Заголовки, названия карточек |
| `--tg-theme-button-color` | ~10 | Кнопки, активные состояния, бордеры |
| `--tg-theme-bg-color` | ~8 | Фон приложения, hover состояния |
| `--tg-theme-link-color` | ~5 | Хлебные крошки, ссылки |
| `--tg-theme-button-text-color` | ~4 | Текст на кнопках |

---

## 2. Новая цветовая схема: две темы

### Определение темы

Тема определяется из настроек Telegram пользователя:
```typescript
// useTelegram.ts
const colorScheme = webApp?.colorScheme || 'light'; // 'light' | 'dark'
```

В CSS применяется через класс на `.app`:
```typescript
// App.tsx
<div className={`app ${colorScheme === 'dark' ? 'dark' : ''}`}>
```

---

### Light Theme — Clean White

Чистый белый фон с teal акцентами. Минимализм и контраст.

| Переменная | Значение | Описание |
|------------|----------|----------|
| `--tg-theme-bg-color` | `#ffffff` | Чистый белый фон |
| `--tg-theme-text-color` | `#212529` | Тёмный текст (почти чёрный) |
| `--tg-theme-hint-color` | `#6c757d` | Серый для мета-текста |
| `--tg-theme-link-color` | `#26a69a` | Teal для ссылок |
| `--tg-theme-button-color` | `#26a69a` | Teal для кнопок |
| `--tg-theme-button-text-color` | `#ffffff` | Белый текст на кнопках |
| `--tg-theme-secondary-bg-color` | `#f8f9fa` | Светло-серый фон карточек |

**Дополнительные переменные:**

```css
:root {
  /* Акцентный цвет */
  --accent-color: #26a69a;
  --accent-color-hover: #00897b;
  --accent-color-light: #e0f2f1;
  
  /* Карточки */
  --card-bg: #f8f9fa;
  --card-border: #e9ecef;
  --card-hover-bg: #f1faf9;
  --card-hover-border: #26a69a;
  
  /* Иконки */
  --icon-gradient: linear-gradient(135deg, #4db6ac 0%, #26a69a 100%);
  --icon-text-color: #ffffff;
  
  /* Тени */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 2px 8px rgba(0, 0, 0, 0.08);
  --shadow-lg: 0 4px 16px rgba(0, 0, 0, 0.1);
  
  /* Разделители */
  --divider-color: #e9ecef;
}
```

---

### Dark Theme — Dark Mode

Тёмный фон с светлым teal акцентом. Комфорт для глаз.

| Переменная | Значение | Описание |
|------------|----------|----------|
| `--tg-theme-bg-color` | `#1a1a2e` | Тёмно-синий фон |
| `--tg-theme-text-color` | `#e8e8e8` | Светлый текст |
| `--tg-theme-hint-color` | `#888888` | Приглушённый серый |
| `--tg-theme-link-color` | `#4db6ac` | Светлый teal для ссылок |
| `--tg-theme-button-color` | `#4db6ac` | Светлый teal для кнопок |
| `--tg-theme-button-text-color` | `#1a1a2e` | Тёмный текст на кнопках |
| `--tg-theme-secondary-bg-color` | `#16213e` | Тёмный фон карточек |

**Дополнительные переменные:**

```css
.app.dark {
  /* Акцентный цвет */
  --accent-color: #4db6ac;
  --accent-color-hover: #80cbc4;
  --accent-color-light: rgba(77, 182, 172, 0.15);
  
  /* Карточки */
  --card-bg: rgba(255, 255, 255, 0.05);
  --card-border: rgba(255, 255, 255, 0.08);
  --card-hover-bg: rgba(77, 182, 172, 0.1);
  --card-hover-border: rgba(77, 182, 172, 0.3);
  
  /* Иконки */
  --icon-gradient: linear-gradient(135deg, #4db6ac 0%, #26a69a 100%);
  --icon-text-color: #1a1a2e;
  
  /* Тени */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.2);
  --shadow-md: 0 2px 8px rgba(0, 0, 0, 0.3);
  --shadow-lg: 0 4px 16px rgba(0, 0, 0, 0.4);
  
  /* Разделители */
  --divider-color: rgba(255, 255, 255, 0.1);
}
```

---

### Сводная таблица цветов

| Элемент | Light | Dark |
|---------|-------|------|
| **Фон приложения** | `#ffffff` | `#1a1a2e` |
| **Фон карточки** | `#f8f9fa` | `rgba(255,255,255,0.05)` |
| **Текст основной** | `#212529` | `#e8e8e8` |
| **Текст вторичный** | `#6c757d` | `#888888` |
| **Акцент (кнопки, иконки)** | `#26a69a` | `#4db6ac` |
| **Акцент hover** | `#00897b` | `#80cbc4` |
| **Бордер карточки** | `#e9ecef` | `rgba(255,255,255,0.08)` |
| **Бордер hover** | `#26a69a` | `rgba(77,182,172,0.3)` |
| **Разделители** | `#e9ecef` | `rgba(255,255,255,0.1)` |

---

## 3. План доработки по файлам

### Фаза 1: Глобальные стили и темы

#### `src/index.css`
- [ ] Добавить подключение шрифта Plus Jakarta Sans
- [ ] Убрать лишние базовые стили

#### `src/App.css`
- [ ] Обновить `:root` — Light Theme (Clean White)
- [ ] Обновить `.app.dark` — Dark Theme
- [ ] Добавить кастомные переменные (`--accent-color`, `--card-bg`, etc.)
- [ ] Обновить спиннер с новыми цветами

#### `src/App.tsx`
- [ ] Получать `colorScheme` из `useTelegram()`
- [ ] Применять класс `dark` к `.app` если тёмная тема

#### `src/hooks/useTelegram.ts`
- [ ] Экспортировать `colorScheme` из WebApp

---

### Фаза 2: Карточки

#### `src/components/SectionCard.css`
- [ ] Использовать `var(--card-bg)` и `var(--card-border)`
- [ ] Добавить hover-эффект с `var(--card-hover-bg)` и `var(--card-hover-border)`
- [ ] Стилизовать `.section-icon` с `var(--icon-gradient)`
- [ ] Убрать Soft UI тени (переходим на Clean стиль)

#### `src/components/ItemCard.css`
- [ ] Аналогичные изменения как в SectionCard
- [ ] Разные цвета иконок по типу контента (text, link, file, image)

---

### Фаза 3: Модалки

#### `src/components/AddSectionModal.css`
- [ ] Обновить фон модалки
- [ ] Стилизовать инпут в Soft UI стиле
- [ ] Обновить кнопку с градиентом и тенью

#### `src/components/AddItemModal.css`
- [ ] Обновить селектор типов (`.type-option`) — Soft UI стиль
- [ ] Обновить выбранное состояние
- [ ] Стилизовать file input

#### `src/components/ItemViewModal.css`
- [ ] Обновить фон и стиль контента
- [ ] Обновить кнопки действий

---

### Фаза 4: Навигация

#### `src/components/Header.css`
- [ ] Обновить фон хедера (можно полупрозрачный с blur)
- [ ] Стилизовать поле поиска в Soft UI
- [ ] Обновить иконку поиска

#### `src/components/CollapsedBreadcrumbs.css`
- [ ] Обновить цвета хлебных крошек
- [ ] Стилизовать dropdown меню

#### `src/components/Breadcrumbs.css`
- [ ] Обновить стили (если используется)

---

### Фаза 5: Кнопки действий

#### `src/components/BottomActions.css`
- [ ] Градиентные кнопки с тенью
- [ ] Hover/active эффекты
- [ ] Возможно: стеклянный фон панели

---

### Фаза 6: Поиск

#### `src/components/SearchBar.css`
- [ ] Soft UI стиль поля ввода

#### `src/components/SearchResults.css`
- [ ] Стилизовать результаты как карточки

---

### Фаза 7: Страницы

#### `src/pages/HomePage.css`
- [ ] Обновить заголовок
- [ ] Стили пустого состояния

#### `src/pages/SectionPage.css`
- [ ] Обновить заголовки секций
- [ ] Стили пустого состояния

---

## 4. Иконки

### Текущее состояние
Используются emoji: 📁, 📝, 🔗, 📄, 🖼, ›

### Решение: MUI Icons (@mui/icons-material)

**Установка:**
```bash
npm install @mui/icons-material @mui/material @emotion/react @emotion/styled
```

**Почему MUI Icons:**
- Зрелая библиотека, хорошо документирована
- Отличная интеграция с React (компоненты)
- 2000+ иконок в 5 стилях (Filled, Outlined, Rounded, Sharp, Two-Tone)
- Единый дизайн-язык

### Динамические иконки папок

**Логика:** иконка папки зависит от содержимого раздела

| Состояние | Иконка MUI | Компонент |
|-----------|------------|-----------|
| Пустая папка | `folder` | `<FolderIcon />` или `<FolderOutlinedIcon />` |
| Папка с содержимым | `folder_open` | `<FolderOpenIcon />` или `<FolderOpenOutlinedIcon />` |

**Реализация в SectionCard.tsx:**

```tsx
import FolderIcon from '@mui/icons-material/Folder';
import FolderOpenIcon from '@mui/icons-material/FolderOpen';

// Определяем, пустой ли раздел
const isEmpty = !section._count || 
  (section._count.children === 0 && section._count.items === 0);

// Выбираем иконку
const IconComponent = isEmpty ? FolderIcon : FolderOpenIcon;

// Использование
<IconComponent sx={{ fontSize: 24, color: 'inherit' }} />
```

### Иконки для типов контента

| Тип | Иконка MUI | Компонент | Цвет фона |
|-----|------------|-----------|-----------|
| Заметка (text) | `description` | `<DescriptionIcon />` | #66bb6a (зелёный) |
| Ссылка (link) | `link` | `<LinkIcon />` | #29b6f6 (голубой) |
| Файл (file) | `insert_drive_file` | `<InsertDriveFileIcon />` | #ffa726 (оранжевый) |
| Изображение (image) | `image` | `<ImageIcon />` | #ec407a (розовый) |

**Реализация в ItemCard.tsx:**

```tsx
import DescriptionIcon from '@mui/icons-material/Description';
import LinkIcon from '@mui/icons-material/Link';
import InsertDriveFileIcon from '@mui/icons-material/InsertDriveFile';
import ImageIcon from '@mui/icons-material/Image';

const iconMap = {
  text: DescriptionIcon,
  link: LinkIcon,
  file: InsertDriveFileIcon,
  image: ImageIcon,
};

const IconComponent = iconMap[item.type];
```

### Иконки навигации

| Элемент | Иконка MUI | Компонент |
|---------|------------|-----------|
| Стрелка вправо (карточка) | `chevron_right` | `<ChevronRightIcon />` |
| Назад | `arrow_back` | `<ArrowBackIcon />` |
| Поиск | `search` | `<SearchIcon />` |
| Добавить | `add` | `<AddIcon />` |
| Закрыть | `close` | `<CloseIcon />` |
| Домой | `home` | `<HomeIcon />` |

### Файлы для изменения

- [ ] `package.json` — добавить зависимости MUI
- [ ] `src/components/SectionCard.tsx` — заменить emoji на MUI иконки
- [ ] `src/components/ItemCard.tsx` — заменить emoji на MUI иконки по типу
- [ ] `src/components/SearchResults.tsx` — MUI иконки с учётом пустоты для разделов
- [ ] `src/components/Header.tsx` — иконка поиска
- [ ] `src/components/BottomActions.tsx` — иконка добавления
- [ ] `src/components/CollapsedBreadcrumbs.tsx` — иконка домой, стрелки

---

## 5. Типографика

### Рекомендуемые шрифты

**Основной:** Plus Jakarta Sans или DM Sans
- Современный, читаемый
- Хорошо работает на мобильных

**Подключение:**
```html
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
```

```css
body {
  font-family: 'Plus Jakarta Sans', -apple-system, BlinkMacSystemFont, sans-serif;
}
```

---

## 6. Анимации

### Текущее
- `transform: scale()` на hover/active
- `transition: 0.2s`

### Обновить
- `translateY(-2px)` на hover карточек
- Изменение border-color на hover
- Timing: `ease` или `cubic-bezier(0.4, 0, 0.2, 1)`
- Длительность: `0.2s`

```css
.card {
  transition: transform 0.2s ease, 
              border-color 0.2s ease, 
              background-color 0.2s ease;
}

.card:hover {
  transform: translateY(-2px);
  border-color: var(--accent-color);
  background-color: var(--card-hover-bg);
}
```

---

## 7. Порядок выполнения

1. **package.json** — добавить MUI зависимости
2. **index.html** — подключить Google Fonts
3. **App.css** — Light и Dark темы, переменные
4. **App.tsx** — применение темы из Telegram
5. **SectionCard.tsx + .css** — MUI иконки, новые стили
6. **ItemCard.tsx + .css** — MUI иконки, цвета по типам
7. **BottomActions.css** — стили кнопок
8. **Header.tsx + .css** — MUI иконка поиска
9. **Модалки** — AddSectionModal, AddItemModal, ItemViewModal
10. **Остальное** — поиск, хлебные крошки, страницы

---

## 8. Проверка

После каждого этапа:
- [ ] Проверить Light тему (по умолчанию)
- [ ] Проверить Dark тему (переключить в настройках Telegram)
- [ ] Проверить на мобильном размере
- [ ] Проверить hover/active состояния
- [ ] Проверить читаемость текста
- [ ] Проверить контраст иконок

---

## Примечания

- Telegram может переопределять CSS-переменные `--tg-theme-*` — используем fallback
- Light тема по умолчанию если Telegram не передаёт `colorScheme`
- MUI иконки добавляют ~200KB к bundle (tree-shaking помогает)
- Тестировать в реальном Telegram клиенте для проверки тем
