# API URL для бэкенда
#
# Локальная разработка (localhost):
#   Можно оставить пустым или не создавать .env — Vite proxy перенаправит на localhost:3000
#
# Мобильное тестирование (cloudflared):
#   Указать URL бэкенд-туннеля, например:
#   VITE_API_URL=https://xxx-xxx.trycloudflare.com
#
# Production:
#   Указать URL продакшн API

VITE_API_URL=
